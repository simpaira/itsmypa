{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the ItsMyPA shell window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
